# Favorite Dua — AI Agent Handoff

## Goal
Flutter mobile app foundation for an offline-first personal Dua library. The source brief is `C:\\Users\\Supreme Traders\\Downloads\\mydua_ocr.txt`.

## Implemented scope (Step 1)
- Flutter + Material 3, light/dark/system themes
- Riverpod dependency injection and theme state
- `go_router` stateful bottom navigation: Home, Library, Add, Favorites, Settings
- Home category grid and navigable category pages
- Add flow choices, manual reusable form, image preview placeholder, editable OCR review skeleton
- Dua detail skeleton and RTL-aware `DuaTextView`
- Drift/SQLite schema for Duas, Categories, Tags, many-to-many DuaTags, Recordings, and OCR metadata
- Database stored under Application Documents/favorite_dua/database/favorite_dua.sqlite
- Starter category seeding on first database creation
- Domain entities, repository contracts, local Dua data pipeline, use cases
- Provider-neutral interfaces for OCR, audio, files, backup, recitation analysis, and speech
- Placeholder-only built-in data asset; no religious content invented

## Intentional boundaries
Do not implement real OCR/audio/cloud/AI in Step 1. CRUD UI persistence, category/tag management, and search behavior belong to subsequent steps. Device media stays outside SQLite; only paths belong in tables. UUIDs are used from the start.

## Generated code
`lib/core/database/app_database.g.dart` is generated by Drift. Regenerate after table changes:

```sh
dart run build_runner build --delete-conflicting-outputs
```

## Verification
Run `flutter analyze` and `flutter test`. For a device/emulator, run `flutter run`.

## Architecture rules
Keep feature-first `data/domain/presentation` boundaries. Widgets never call Drift or device plugins directly. Flow is UI → controller → use case → repository contract → local data source → Drift. Keep Arabic/Urdu direction local to the text widget; never force the whole app RTL based on Dua content.

## Next recommended task
Step 2: complete local Dua CRUD plus real category/tag repositories and controllers, then add focused repository/controller/widget tests.

## Added category content
- Seeded Morning and Evening remembrances (Sunan Abi Dawud 5068).
- Seeded After Prayer remembrance (Sahih Muslim 591).
- Category screens now query Drift through the repository and display Dua cards.
- Seeding is idempotent and also runs for existing local databases; schema version is 2.

## Daily Android reminders
- Settings includes an enabled switch and time picker for one daily local reminder.
- Android 13+ permission is requested only when the user enables reminders.
- Reminder preferences persist with SharedPreferences and reschedule after time changes/reboot.
- Uses timezone-aware, inexact alarms; no exact-alarm permission is required.

## OCR implementation (current)
- Camera/gallery images are decoded, EXIF orientation is applied, large images are resized, and grayscale/contrast preprocessing is performed before OCR.
- Offline Tesseract runs the combined `ara+urd+eng` model and retries Arabic, Urdu, and English separately if the combined result is empty.
- Empty or unreadable images now show actionable errors instead of silently producing a blank field.
- Extracted text is passed into the manual Dua form through the `ocrText` route parameter.
- Drift/SQLite `TEXT` columns preserve Unicode text; no lossy encoding conversion is performed.
- Bundled offline recognition currently includes Arabic, Urdu, and English. Supporting every written language requires adding its Tesseract traineddata file (or a cloud OCR provider); Unicode storage itself supports all languages.

## Advertising (test configuration)
- Google Mobile Ads SDK is initialized at startup.
- Android uses Google's official test app ID `ca-app-pub-3940256099942544~3347511713`.
- Banner uses test unit `ca-app-pub-3940256099942544/6300978111`.
- Interstitial uses test unit `ca-app-pub-3940256099942544/1033173712`.
- Interstitial is eligible after every third pushed screen, not on every navigation, and is preloaded again after dismissal.
- User-provided production IDs are intentionally not active while test ads are requested.

## Active task: Personal Dua media library
- Goal: local Dua text, document, image, video, and audio library.
- Required: direct import and Android share-to-app, private file copies, searchable removable tags, deletion, outbound Android sharing to WhatsApp/Teams/etc.
- OCR is only for image/document items. Video and audio require native in-app players.
- Dependencies already added: file_picker, share_plus, video_player, just_audio, open_filex, receive_sharing_intent, mime.
- No media-library feature files or database table exist yet; the creation command was interrupted and verification confirmed nothing was written.
- NEXT STEP: create LibraryItems Drift table, schema v3 migration, then repository/UI/share receiver/players/tags/search/delete/share-out.
- After implementation: regenerate Drift, analyze, test, build, install, then replace this checkpoint with verified results.
- Added requirement: OCR-created image/document records must support searchable tags.
- Added requirement: users can attach one or more recorded/imported audio voice notes or recitations to an OCR item.
- Attached audio must support playback, removal, and outbound sharing, and remain linked to its parent OCR/library item.
- Schema implication: add LibraryItems plus MediaAttachments (or equivalent parentId relationship); do not store audio bytes in SQLite, only private copied file paths and metadata.
- Clarification: audio attachments apply to EVERY Dua, not only OCR-created items.
- Built-in, manually typed, scanned/OCR, image-backed, and document-backed Duas may each have multiple recorded or imported audio attachments.
- Each attachment must support playback, optional rename/label, outbound sharing, and removal without deleting the parent Dua.
- Deleting a parent Dua/library item must also remove its private attachment files and database attachment rows.
- Recommended unified relationship: MediaAttachments(parentType, parentId, kind, filePath, mimeType, title, durationMs, createdAt).
- Added requirement: EVERY Dua may also have multiple web-link attachments.
- Link types: YouTube and general website URL.
- YouTube URL UI: recognize valid YouTube/youtu.be URLs, show a video-style preview card (thumbnail/title when metadata is available), and open the YouTube app or browser when tapped.
- General website URL UI: show a link card with readable host/title and launch the external website when tapped.
- Users can add, rename/label, share, and remove links without deleting the parent Dua.
- Validate URLs and allow only http/https before launching; never execute arbitrary URI schemes.
- Schema implication: use unified Attachments rows supporting audio/file/link kinds, parentType + parentId, URL/filePath, MIME type, display title, duration, and timestamps.
- UI implication: every Dua detail screen needs Attachments sections for Audio and Links plus Add Audio and Add Link actions.

## Sharing/media library implementation checkpoint (2026-09-20)
- Implemented Drift LibraryItems table and schema migration 2 -> 3.
- Implemented direct multi-file import with private copies under Application Documents/favorite_dua/library/<uuid>.
- Implemented Android SEND, SEND_MULTIPLE, and content VIEW intent filters; launchMode is singleTask.
- Implemented cold-start and while-running receive_sharing_intent processing for files, text, and URLs.
- Implemented Library search over title/tags/OCR/text, and All/Image/Video/Audio/Document/Text filters.
- Implemented item detail: image preview, video_player playback, just_audio playback, external document opening, tag editing, outbound share sheet, and deletion of DB row plus private file.
- receive_sharing_intent is pinned/vendored at plugins/receive_sharing_intent (1.8.1) with JVM 17 compatibility because 1.9.0 required Android SDK 37.
- Verification: Flutter tests passed; debug APK built; installed successfully on RMX3760 serial 0H74119I23101F6F and launched.
- APK: build/app/outputs/flutter-apk/app-debug.apk.
- NOT YET IMPLEMENTED: universal per-Dua audio attachments/recording, YouTube preview cards, general website attachment UI, URL launching, linking LibraryItems back to a Dua, or OCR action within the new media detail screen.
- NEXT STEP: add unified Attachments table keyed by parentType/parentId, then Add/Edit and Detail UI for audio and links; add url_launcher and YouTube parsing/thumbnail; connect image/document OCR results to LibraryItems.

## Corrected share-to-Dua workflow (2026-09-20)
- Database schema is now version 4: Duas has tagsText and DuaAttachments stores multiple audio/image/video/document/link/youtube attachments per Dua.
- Incoming non-text Android shares are privately imported then routed to /add/manual?libraryItemId=<id>.
- Add/Edit Dua now loads a shared item, runs OCR for shared images, prefills editable text/title, accepts comma-separated tags, notes, additional files/audio, and validated http/https links, then persists the Dua and copied attachments.
- Detail/View now reads the saved Dua and attachment rows, displays tags/text/notes, plays audio with just_audio, launches YouTube/website externally, opens local files, and shares Dua or attachments.
- OCR now compares original-image and enhanced grayscale/contrast passes and selects the higher-quality result; bundled recognition remains Arabic+Urdu+English and cannot guarantee every character.
- Tests passed and debug APK built successfully.
- Current limitation: PDF/document OCR is not implemented; documents are attached/opened. Microphone recording UI is not implemented; audio files can be imported. Editing an existing saved Dua does not yet hydrate the form. Video attachments open externally from Dua detail; the standalone Library detail has an in-app video player.
- NEXT STEP: implement existing-Dua edit hydration/update, microphone recording, PDF page rendering/OCR, and embedded video/YouTube preview cards.
- OCR multilingual fix: prior quality scorer incorrectly used literal `\\s` and `\\n`; corrected to actual whitespace and newline matching.
- OCR now compares four full-image passes: original/enhanced with `urd+ara+eng` and original/enhanced with `ara+urd+eng`, selecting the result with best visible-character and line coverage.
- This addresses screenshots where Arabic was extracted but Urdu/English lines were omitted. OCR remains editable because decorative/low-resolution text can still be imperfect.

## Revised primary workflow: manual crop instead of required OCR
- OCR is no longer the default or required creation method because recognition quality is not reliable enough.
- Camera/gallery/share image -> interactive crop -> side-by-side source and cropped-result preview -> save crop as a new PNG -> Add/Edit Dua.
- The cropped PNG becomes the primary visual content and must persist in private app storage.
- OCR remains optional only; user can create/save without extracted text.
- Cropped-image Dua supports searchable tags and multiple audio/voice, video, document, YouTube, and website-link attachments.
- Search must find the saved item by title/tags even when it has no OCR text.
- NEXT STEP: add interactive crop UI, PNG export, side-by-side preview, route camera/gallery/shared images through crop, then into attachment-capable Add/Edit screen.

## Manual crop-first flow implemented
- Added custom ImageCropScreen using the existing image package; no external crop dependency is required.
- Crop UI shows original and cropped PNG preview side by side, with horizontal and vertical range controls.
- Camera/gallery Image Preview Continue now routes to /add/crop instead of OCR.
- Incoming Android image shares route directly to /add/crop.
- Use cropped image exports a PNG to temporary storage and opens /add/manual?croppedPath=...
- Add/Edit Dua accepts croppedPath, adds it as the primary image attachment, and copies it into permanent private Dua storage on Save.
- OCR remains available through the older OCR route but is no longer required in the default image flow.
- Attachments supported by Add Dua: image/PNG, imported audio, video, document, and http/https YouTube/website links; tags and notes persist.
- Current crop controls use percentage range sliders rather than draggable crop handles.
- NEXT STEP: build/install on device, test WhatsApp image share -> crop -> add attachments/tags -> save -> detail end-to-end; then add microphone recording and existing-Dua edit hydration.

## Direct recorder and embedded viewers implemented
- Add/Edit Dua includes direct Record/Stop microphone control with elapsed duration.
- After Stop, recording appears as an audio attachment with Play/Pause and Discard. Discard deletes the temporary audio file; Save copies retained audio into private Dua storage.
- Android RECORD_AUDIO permission is declared and requested only when recording starts.
- Added EmbeddedWebScreen using webview_flutter for YouTube and general http/https links, with an external-app/browser fallback button.
- Added DocumentPreviewScreen using pdfrx: PDF files render inside the app. Other attached file formats remain saved and open via a compatible installed phone app.
- All file types selected by the picker can be attached, copied privately, shared, and removed.
- Verification: flutter analyze has no application compile errors (only warnings/info); flutter test passes.
- NEXT STEP: build/install on RMX3760 and perform device tests for microphone permission, record/play/discard/save, WebView YouTube, website navigation, PDF preview, and unsupported document fallback.

## Inbound share restriction and multi-share queue
- Android Share-to-app accepts ONLY image/* and application/pdf for SEND, SEND_MULTIPLE, and content VIEW.
- Runtime receiver independently rejects every non-image/non-PDF item.
- Fixed shared-image crop route to include the encoded source path.
- Multiple shared images/PDFs open a Share Review queue. Each item is a separate pending Dua request.
- User can Create separate Dua, Discard one, or Discard all. Images route to crop; PDFs route to Add Dua with attachment.
- Audio/video/other documents/links remain attachable manually inside Add/Edit Dua but are not accepted as incoming shares.
- Verification: Flutter tests pass. NEXT STEP: build/install and device-test single/multiple image and PDF shares.

## Focused initial crop/voice/schedule flow (2026-09-20)
- Initial Add flow is intentionally limited to Camera or Gallery; both proceed to image preview and manual crop.
- Fixed `/add/manual` routing so `croppedPath`, `libraryItemId`, and optional text are preserved. The cropped PNG now reaches the Save screen.
- Save screen now focuses on: cropped image preview, Dua name, direct microphone recording or audio selection, audio play/pause review, discard, daily time, and repeat count (default 3).
- A voice is required before saving. The cropped image and chosen/recorded voice are copied to private Application Documents storage and linked to the new Dua in Drift.
- Added native Android `DuaAlarmScheduler`, `DuaAlarmReceiver`, and foreground `DuaPlaybackService` connected through MethodChannel `favorite_dua/audio_schedule`.
- At the selected daily time Android starts low-volume speech/media playback at 25% app-player volume and repeats the saved recording 1-5 times (default 3), including with the screen off.
- The receiver schedules the next day after firing. Schedule metadata persists in Android SharedPreferences and is restored after boot or app replacement.
- Added exact-alarm and foreground media-playback manifest declarations. On Android versions/devices that do not grant exact-alarm access, the implementation safely falls back to an allow-while-idle alarm, which the OS may deliver slightly late.
- Text-to-speech was removed from this focused initial version; “voice” means a user recording or selected audio file.
- Verification: Flutter analysis has no application compile errors (only existing warnings/info), all Flutter tests pass, and debug APK builds successfully at `build/app/outputs/flutter-apk/app-debug.apk`.
- NEXT DEVICE CHECK: verify microphone permission, record/play/discard, save, and set a test alarm 2-3 minutes ahead; some OEM battery managers may require allowing auto-start/background activity.

## Ad-free UI and touch crop/listing update (2026-09-20)
- Removed Google Mobile Ads dependency, SDK startup initialization, AdMob manifest app ID, banner widget, and interstitial navigation observer. No ad code or ad placement remains active.
- Replaced percentage-slider crop controls with a touch crop box over the source image.
- User drags inside the box to move the selection and drags any of four visible corner handles to enlarge or reduce it.
- Cropped PNG result preview updates after each drag and `Use selected area` continues to the focused voice/schedule Save screen.
- Home now shows the real saved Dua stream instead of a placeholder card.
- Dua listing cards load their attachments, show the first cropped image, and show direct action chips for Play voice, Play video, View image, Open document, YouTube, and Website.
- Audio plays/pauses from the listing. Documents use the in-app PDF viewer where supported, web/YouTube use the embedded browser, and image/video files open using an installed compatible app.

## Category-only Home and complete attachment options
- Home no longer displays every Dua. It displays only Morning, Evening, and After Prayer category cards plus Add New Dua.
- Tapping a category opens that category's full Dua listing.
- Add/Save Dua now requires selecting Morning, Evening, or After Prayer and persists the category relationship, so the saved Dua appears in the correct category list.
- Save screen supports direct/selected voice, video file, document file, YouTube URL, and general website URL attachments.
- Added links are restricted to http/https and YouTube links are classified separately.
- Local video/document files are copied to private Dua storage; attachment rows are shown as direct action chips in category listings.
- Verification: analysis has no application compile errors, Flutter tests pass, and debug APK builds successfully.

## Custom categories, easier crop, and image-first Dua display
- Home includes Add Category. Users can create persistent custom category names; duplicates are rejected case-insensitively.
- Home category grid and Add Dua category dropdown load all categories dynamically from Drift, including user-created categories.
- Crop screen scrolling is disabled. Long-press inside the selection to move it; long-press a white corner and drag to resize it. This prevents the page from scrolling while adjusting crop handles.
- Cropped Dua image is visible directly inside category listing cards with contained sizing and clipped card boundaries.
- Opening a Dua displays its cropped image at the top by default, bounded to the screen/card; the redundant image attachment row is hidden.

## Custom category deletion and floating Save
- Custom category cards show a delete icon; built-in categories remain protected.
- Deletion requires confirmation. Duas are preserved by clearing their category assignment before deleting only the custom category.
- Add/Edit Dua no longer uses an inline bottom Save button. Save Dua is now an always-accessible extended floating action button with saving progress.
- The form includes extra bottom padding so the floating button does not cover fields.

## Delete saved Dua
- Dua detail AppBar now includes Delete Dua.
- Deletion requires explicit confirmation and names the selected Dua.
- Confirming deletion stops playback, cancels the Dua's Android schedule, removes local image/audio/video/document files, deletes the Dua row, and relies on the existing cascade to remove attachment rows.
- Other Duas and categories are unaffected.

## Optional voice attachment
- Voice recording/phone audio is no longer required to save a Dua.
- A cropped image and category are sufficient; video/document/link attachments also remain optional.
- Daily playback defaults off and its switch is disabled until an audio file exists.
- Audio attachment rows and Android schedules are created only when voice audio is present.
- Verification: no application compile errors, tests pass, debug APK builds successfully.

## Per-Dua scheduled action modes
- Enabling Schedule this Dua now offers: Show notification only, Play Dua voice, or Play voice and show notification.
- Notification-only works without an audio attachment.
- Play and Both modes validate that a voice is attached before saving.
- Notification/Both requests Android notification permission when saved.
- Native alarm metadata persists the selected mode and restores it after reboot/app replacement.
- Alarm receiver posts a high-priority Dua reminder for Notification/Both and starts low-volume repeated foreground playback for Play/Both.
- Android requires a foreground-service notification while background audio is actively playing, even in Play-only mode.

## White colorful theme, exact-time access, and inbound sharing fix
- App is forced to light mode with a white scaffold/app-bar background; category and Dua cards use deterministic pastel colors for visual separation.
- Added `Allow exact timing` inside the per-Dua schedule section. It opens Android's Alarms & reminders access screen when exact alarms are not granted.
- Without exact-alarm access Android may delay the fallback alarm; users must grant access and save the schedule again for precise delivery.
- Incoming image/PDF handling now routes through the global GoRouter after the first frame instead of relying on a builder context that may not contain GoRouter.
- Runtime type detection now accepts common image extensions even when WhatsApp/another provider sends a generic or missing MIME type.
- Share-stream errors are logged and Android onNewIntent now reports the intent as handled.

## Schedule playback diagnosis and fix
- Device inspection showed Favorite Dua alarms were inexact (`window=+1h`) because Android's special exact-alarm app-op was still at default.
- Saved schedule inspection also showed the 22:53 Dua was explicitly stored in `notification` mode, so audio playback was correctly skipped despite repeat=3.
- Enabling Schedule now automatically selects Both when audio is attached, and Notification-only when no audio exists; the user can still change the mode.
- Exact timing still requires Android Alarms & reminders access, available through the new button. A time whose minute has already started is scheduled for the next day.

## Stable per-Dua color palette
- Added shared `DuaPalette` with ten distinct readable pastel colors: pink, amber, mint, blue, lavender, teal, peach, rose, lime, and sky.
- Each Dua receives a deterministic color from its UUID, so different Duas vary while the same Dua keeps its color across launches.
- Category-list Dua card uses the assigned color.
- Dua detail Scaffold and AppBar use exactly the same assigned color.
- General application background remains white for contrast.

## Current-minute scheduling fix and device evidence
- Device at 23:29 showed exact-alarm permission allowed and POST_NOTIFICATIONS granted.
- Stored 23:28 entry had an empty audio path and mode=notification, so voice playback was impossible by configuration.
- Because it was saved after 23:28:00, the old scheduler placed it at 23:28 the next day; dumpsys confirmed the exact next-day trigger.
- Scheduler now treats a selected time matching the current hour/minute as an immediate test-style occurrence and triggers about 5 seconds later, then the receiver schedules the following daily occurrence normally.
- Latest APK with this fix is installed. To test playback, attach voice, enable Schedule (defaults to Both), select the current minute, and save; to test notification-only, no audio is required.

## 2026-09-20 scheduled voice reliability fix
- Device inspection showed recent schedules were saved as 
otification, so they intentionally did not launch voice playback. Users must select **Play Dua voice** or **Play voice and show notification**.
- Android DuaPlaybackService now uses AudioAttributes.USAGE_ALARM instead of USAGE_MEDIA, so timed playback uses the device alarm-volume channel while the app is closed.
- Scheduled voice volume was raised from 25% to 70% for reliable audibility.

## 2026-09-20 schedule persistence and voice gating
- Added Drift schema v5 fields on duas: scheduleEnabled, scheduleHour, scheduleMinute, scheduleRepeats, and scheduleMode. Existing data is preserved by migration.
- Newly saved Duas persist and display the chosen daily time, mode, and voice-repeat count on the detail screen.
- Android sends one notification per scheduled occurrence; only audio repeats according to the selected repeat count.
- Play Voice and Both are disabled when no audio is attached. Recording/selecting audio while scheduling automatically selects Both; removing audio returns the mode to notification-only.
- Device inspection proved prior 23:41 and 23:42 schedules had empty audio paths and notification mode, which is why no voice played.
- Debug APK built, installed successfully on RMX3760, and launched.

## 2026-09-21 existing Dua schedule editing
- Fixed /dua/:id/edit to pass the existing Dua ID into EditDuaScreen.
- Edit now reloads the saved title, category, image, attached audio, schedule enabled state, time, repeat count, and notification/play mode.
- Saving from Edit updates and reschedules the same Dua instead of creating a duplicate; existing audio can be reused without recording again.
- Debug APK built, installed successfully, and launched on RMX3760.

## 2026-09-21 daily alarm loop and category colors
- Fixed a critical loop: when an alarm fired during its selected minute, recurrence previously scheduled another alarm five seconds later. Receiver-triggered recurrence now always targets the next day.
- Result: one notification per daily occurrence; voice alone repeats according to the Dua's saved repeat count.
- Added WAKE_LOCK permission groundwork for reliable screen-off playback.
- Added category-based colors: Morning blue, Evening yellow, Night/Before Sleep indigo, After Prayer/mosque green, and stable varied colors for other categories.
- Category cards, category Dua lists, and Dua detail heading/background now use the same category color.
- Debug APK built, installed successfully, and launched on RMX3760.

## 2026-09-21 YouTube inbound sharing and edit content restoration
- Added Android ACTION_SEND text/plain intent filter so Favorite Dua appears in YouTube and other link share sheets.
- Inbound text shares now enter the share-review queue; creating the request extracts the first http/https URL and attaches it as YouTube or website to a new Dua.
- EditDuaScreen now restores and displays existing Dua text, audio attachment, image, and non-image attachments. Existing voice is reusable for schedule changes without re-recording.
- Existing Dua text is editable and saved back to the same record.
- Debug APK built successfully at build/app/outputs/flutter-apk/app-debug.apk. Installation is pending because RMX3760 disconnected from ADB after the build.

## 2026-09-21 notification content, attachment management, themes, and multilingual catalog
- Scheduled notification payload now persists category and Dua text; notification title is Category • Dua title and expanded body shows the saved Dua text. Old schedules remain compatible but need resaving to gain the new content.
- YouTube text/plain shares are supported and can create a new Dua or attach the video link to any existing Dua through a chooser. Multiple YouTube links are allowed.
- Dua detail supports adding multiple non-YouTube website URLs and document files. YouTube URLs entered manually are rejected with guidance to use YouTube Share. Individual links, YouTube items, and documents can be deleted.
- Fixed link/document opening to pass the actual URL/path into the viewer.
- Light, Dark, and System themes now drive MaterialApp and persist with SharedPreferences.
- Replaced placeholder assets/data/builtin_duas.json with schema v2: 13 UTF-8 Arabic Duas, one per category, English translations, and references. translations is keyed by ISO language code for future language expansion.
- AppDatabase loads the JSON catalog on every open using insert-or-ignore, so existing installs receive new built-ins without losing user data.
- Combined debug APK built, installed successfully, and launched on RMX3760.

## 2026-09-21 $4.99 dummy Remove Ads and test ad cadence
- Added google_mobile_ads 9.1.0 with Google's Android test app ID and test interstitial unit ID; production ad IDs are not active.
- Non-entitled users get an interstitial after a randomized 5 or 6 pointer interactions/navigation taps, never every navigation.
- Settings shows a one-time Remove Ads product priced at $4.99. Current checkout is explicitly a dummy purchase and charges nothing.
- Dummy purchase stores remove_ads_entitlement in SharedPreferences, immediately disables the ad counter, persists after restart, and replaces the purchase row with an Ads removed status.
- shouldShowAdsProvider exposes the entitlement gate for future ad placements/production billing.
- Debug APK built, installed successfully, and launched on RMX3760.

## 2026-09-21 bundle scheduler, link normalization, theme crash, and audio level
- Added a primary Dua Bundle Scheduler reachable from Settings. It selects audio-enabled Duas across categories, assigns each Dua 1-10 repeats, and schedules the bundle daily, weekly, or monthly.
- Added native Android bundle persistence, exact alarms, reboot/package-replace restoration, one notification per occurrence, and sequential background audio playback.
- Website link entry now accepts a host without a scheme by normalizing it to https:// before validation; manually entered YouTube links remain intentionally rejected in favor of YouTube Share.
- Fixed the dark-theme crash risk: MaterialApp no longer force-unwraps a transiently null Router child, and asynchronous theme preference loading is lifecycle-safe and guarded against storage errors.
- Improved direct voice recording with AAC-LC mono at 44.1 kHz/128 kbps plus automatic gain, noise suppression, and echo cancellation.
- Scheduled Dua playback volume was raised from 70% to 100%. It uses Android's alarm audio stream, so the phone's Alarm volume setting still controls final loudness.
- Flutter widget tests pass; debug APK builds successfully and the latest APK is installed on RMX3760.
## 2026-09-21 UTF-8 per-Dua JSON and expanded verified catalog
- Added DuaJsonService. Every newly created or edited Dua writes `favorite_dua/duas/<dua-id>/dua.json` in UTF-8 with category, Arabic text, translation, transliteration, reference, notes, tags, image path, voice paths, all attachments, schedule settings, favorite/built-in flags, and timestamps. SQLite remains the primary index for reliable search and scheduling.
- Expanded assets/data/builtin_duas.json to schema v3 with 130 entries: exactly 10 Duas in each of Morning, Evening, Night, Before Sleep, After Prayer, Protection, Travel, Forgiveness, Family, Health, Rizq, Qur'anic Dua, and Personal.
- Each catalog entry includes Arabic, English translation, transliteration, source reference, source collection, and source category. Required-field validation found 0 missing Arabic/translation/reference values.
- Source corpus: open Hisnul Muslim structured dataset from sehalhussain/Hadith-Dua-assets; source references retained per entry. Generator retained at tool/generate_dua_catalog.ps1 and downloaded source at assets/data/hisnul_muslim_source.json for audit/reproducibility.
- AppDatabase now imports transliteration from the catalog in addition to Arabic, English translation, and reference.
- Flutter widget tests passed and debug APK built successfully. Installation is pending because RMX3760 disconnected from ADB.
## 2026-09-21 latest UI behavior, font sizing, and deployment policy
- Category Dua lists now show the Dua text only; English translation was removed from list cards.
- Dua detail now presents translation in a separate, clearly labelled Translation section below the Arabic/Dua text.
- Fixed image attachment leakage after deleting a Dua in a category (observed in Morning): DuaCard instances now use ValueKey(dua.id), and state reloads attachments when the Dua ID changes. Detail queries remain scoped to the selected Dua ID.
- Added persistent global font-size settings under Settings > Text size: Small (0.9, default), Medium (1.0), Large (1.15), and Extra Large (1.35). Scaling applies to Dua text, translations, settings, and navigation text through MediaQuery/TextScaler.
- Font preference is stored in SharedPreferences and restored safely; storage failures retain the current-session selection.
- Static analysis for the modified font-size files reports no issues, and Flutter widget tests pass.
- IMPORTANT USER POLICY: Do not install/deploy automatically. Build and validate locally only. Install or launch on the attached device only when the user explicitly asks to deploy/run/install.
- Engineering practice: preserve SQLite as the indexed source of truth; keep UTF-8 per-Dua JSON snapshots for portability; validate generated catalog counts/required fields; use stable widget keys for stateful list items; persist user preferences; run formatter, focused analysis, tests, and APK build proportional to changes before reporting completion.
- The latest font-size/list-translation changes have NOT been deployed to the device.
## 2026-09-21 — Latest Dua navigation, ordering, favorites, and bundle behavior

- A Dua card tap opens its detail page; the chevron arrow is now also an explicit button that opens the same detail page.
- Dua queries are ordered by `createdAt DESC`, so newly added Duas appear first in category and general lists.
- Dua detail app bar now has a Favorite toggle (heart/outlined heart) that persists `isFavorite` in Drift.
- Bundle Scheduler shows only Duas with attached voice audio; Duas without voice are hidden because they cannot be played.
- If no voice-enabled Dua exists, Bundle Scheduler displays guidance to attach a voice first.
- Selected bundle Duas retain their per-Dua repeat selector (1–10) and daily/weekly/monthly schedule controls.
- Focused Flutter analysis completed with no errors; only three pre-existing style-level curly-brace info notices remain.
- These changes were not deployed after editing; deploy only when the user explicitly asks.